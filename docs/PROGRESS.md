# 진행 상황

> 세션 시작 시 이 파일부터 읽는다. 작업 종료 시 `/wrap`으로 갱신한다.
> 목적: 다음 세션이 코드베이스를 재탐색하지 않게 하는 것. 사실만 짧게.

## 현재 위치

**M1~M6 완료.** 테스트 **259개** 통과, `tsc --noEmit` 클린, `npm run build` 클린.
브라우저에서 새로고침 복구 · 판본 찍기 · 산출물 선택 다운로드 실측 확인.

**개정안 #02 서버 불필요 항목 대부분 완료** (`docs/SPEC-amendment-02.md` §E 표에 항목별 현황).
E1(원칙 4 개정)·E2(점검 화면)·E3(산출물 8종)·E4(문서 보관함) 구현·커밋 완료.
**E5(자료 첨부) 미착수, E6(UI/UX) 2/5.**

**M7 8턴 진행** (2026-09-02). 결함 3건 발견 — 2건 수정·커밋(`55265a5`), 1건은 빌더 판단 대기.
검증기 오탐(`-링` 접미), 컨텍스트 유실 시 인용 날조, 5턴 만에 완성도 100% 도달.

다음 할 일:
1. **M7 계속** — 8/30턴. 남은 22턴에서 잡을 것: 장기 세션의 컨텍스트 유실,
   `trimHistory` 실동작, 25턴 넘김 안내(FR-012 AC2), 압축본 4,000자 상한 적정성(§13-3).
2. E6 나머지 3건(모르겠어요 버튼 · 시작 화면 · S0 완화) — M7에서 실제로 걸리는 것부터.
3. E5(자료 첨부)는 그 다음. 없어도 앱은 성립한다.
4. E7~E10(계정·결제)은 **확정 제약 변경**이라 빌더 승인 없이 착수 불가.

실행: `npm run dev` → http://localhost:5173

```
index.html / vite.config.ts        Vite + React 진입점
src/main.tsx                      루트 마운트
src/styles.css                    §10 레이아웃 (플레인 CSS, 라이트/다크 대응)
src/ui/App.tsx                    useReducer 상태 + 2단 레이아웃 + runTurn 연결
src/ui/Header.tsx                 턴 수·추정 비용·설정 (FR-012)
src/ui/ChatPanel.tsx              대화, 로딩, 오류별 안내, 잠금 알림 (FR-001)
src/ui/PreviewPanel.tsx           섹션 상태색·접기/펴기·직접 편집 (FR-006, FR-007)
src/ui/IssuePanel.tsx             검증 이슈 목록 + 진행률 + 점검 화면 열기 (FR-005)
src/ui/ExportGate.tsx             내보내기 전 점검 2단(점검→받기) (FR-005 개정)
src/ui/ExportBar.tsx              md 3종 복사·다운로드·미리보기 (FR-008)
src/ui/LibraryPanel.tsx           문서 목록·복제·삭제·백업·판본 이력 (FR-016)
src/ui/Settings.tsx               BYOK 키 · JSON 내보내기/가져오기 (FR-011)
src/ui/QuestionCards.tsx          객관식 질문 카드 (FR-014)
src/storage/persist.ts            KV 인터페이스 + migrate() + 키 저장 (FR-010)
src/storage/library.ts            보관함 — 목록·판본·백업 (FR-016)
src/export/render.ts              상태 → 마크다운 조립 (FR-008/009/013)
src/export/artifacts.ts           산출물 8종 카탈로그 — UI는 이 배열만 안다
src/export/html.ts                PRD.html · overview.html (자립형, 외부 요청 0)
src/export/markdown.ts            최소 마크다운 → HTML (이스케이프 먼저)
src/export/zip.ts                 무압축 ZIP 작성기 (의존성 없이)
src/export/download.ts            Blob 저장 공용 경로

src/config.ts                     모델·단가·튜닝 값 단일 출처
src/types/prd.ts                  §4 타입 + 섹션 정의 + 빈 상태 팩토리
src/validator/vendorDict.ts       §6.3 벤더 사전 + 미태깅 탐지
src/validator/validate.ts         §6.1 미완성 10 + §6.4 경고 3 + completeness()
src/engine/patch.ts               §5.2 패치 타입 + 런타임 검사 (LLM 출력은 불신)
src/engine/applyPatches.ts        FR-003 적용 엔진 + editSection/unlockSection
src/engine/systemPrompt.ts        §5.2 엔진 지침 + 스키마 위반 재요청 문구
src/engine/prompt.ts              §5.1 조립 — 상태 JSON + 최근 6턴 + 입력
src/engine/geminiAdapter.ts       responseSchema + 호출 + 벤더형식→정규 Patch[]
src/engine/callEngine.ts          재시도 격리 + runTurn(한 턴 왕복)

테스트 259개 — validate 58 · persist 33 · render 31 · library 30 · artifacts 25 ·
applyPatches 24 · callEngine 24 · question 16 · markdown 13 · zip 7.
live.smoke(1)는 GOOGLE_API_KEY 있을 때만.
```

실제 호출은 `GOOGLE_API_KEY=... npx vitest run live.smoke` 로 돌린다.

## 마일스톤

| # | 내용 | 상태 |
|---|---|---|
| M0 | 에이전트/문서 환경 세팅 | ✅ 2026-08-27 |
| M1 | `PRDState` 타입 + `validate()` + 단위 테스트 (UI보다 먼저) | ✅ 2026-08-27 |
| M2 | `applyPatches()` + 테스트 (LLM 없이 목업 패치로) | ✅ 2026-08-27 |
| M3 | LLM 연동 — 프롬프트 조립/호출/파싱/재시도 | ✅ 2026-08-27 |
| M4 | UI 셸 — 채팅 + 미리보기 2단 + 검증 이슈 패널 | ✅ 2026-08-27 |
| M5 | 영속화 + BYOK 설정 화면 | ✅ 2026-08-27 |
| M6 | 내보내기 — 전체본 + 압축본 + 세팅 지침 렌더러 | ✅ 2026-08-27 (M5보다 먼저) |
| M7 | 실사용 검증 — 이 앱으로 이 앱을 다시 기획 (30턴+) | 🟡 **8턴 진행.** 결함 3건 발견, 2건 수정 |

## 결정 기록

`Suggestion:` 항목을 변경했거나 Open Question을 확정했다면 여기에 한 줄씩.

| 날짜 | 항목 | 결정 | 사유 |
|---|---|---|---|
| 08-27 | **엔진 모델** | `claude-opus-5` 단일 확정. $5/$25 per 1M, 단가 검증됨 | 빌더 확정. 멀티벤더는 v2 백로그 |
| 08-27 | **FR-013 신설** | 개발 환경·에이전트 세팅 지침 생성 (우선순위 Should, FR-008 의존) | PRD만 받으면 "뭘 먼저 만들지"가 비어 바이브코딩이 산만해짐 |
| 08-27 | FR 12개 상한 초과 | §6.4 경고를 알고 넘어감 | 빌더 승인 |
| 08-27 | FR-012 우선순위 | Should → Must | 비용 계측은 나중 과금 미터의 기반 |
| 08-27 | `PRDState` 필드 | `schemaVersion` 추가 | FR-010 영속화 마이그레이션 기준점 |
| 08-27 | LLM 호출 구조 | 어댑터 뒤로 격리. UI는 `callEngine()` 하나만 앎 | 벤더 추가·서버 프록시 전환 비용을 낮춤 |
| 08-27 | M3 구현 방침 | structured outputs로 JSON 강제 + `effort` 낮춤 | 전자는 FR-004를 폴백으로 격하, 후자는 thinking 토큰 비용 통제 |
| 08-27 | 캐싱 판단 정정 | "효과 미미" → "섹션 순서 고정 시 앞쪽부터 캐시 히트" | 초기 판단이 과소평가였음 |
| 08-27 | ~~멀티벤더 선택 UI~~ | **철회.** 스코프 오해였음 | 개정안 `SPEC-amendment-01.md` 상단 참조 |
| 08-27 | M1 · 벤더 사전 오탐 (§13 Q4) | 버전 패턴을 **벤더 키워드 인접 시에만** 인정. `meta`/`together`/`google`은 일반어 충돌로 제외 | 스펙 §6.3 원안 `/\b[a-z]+[-\s]?\d+/i`를 단독 적용하면 `FR-001`·`ES2022`까지 잡힘 |
| 08-27 | M1 · 한글 정규식 | 조건절 판정에서 `\b` 제거 | JS `\b`는 ASCII 기준이라 한글 뒤에서 **절대** 매칭 안 됨. 조건절 AC가 전부 오차단되던 버그 |
| 08-27 | M1 · S9 필수 여부 | `SECTION_DEFS.S9.required = false` | 조건부 필수는 `MONETIZATION_NO_COST`가 따로 잡는다 (스펙 §4.2 주의) |
| 08-27 | M1 · 프로젝트 셋업 | TypeScript + Vitest만. Vite/React는 M4에서 추가 | M1~M3은 UI가 없어 불필요 |
| 08-27 | **§13 Q2 해소** | 사용자 편집 섹션을 보호한다. `Section.locked` 플래그 | 빌더 확정. 손으로 고친 걸 LLM이 말없이 덮어쓰면 신뢰가 무너짐 |
| 08-27 | M2 · 잠금 방식 | **영구 잠금이 아닌 해제 가능한 잠금.** 거부 시 `rejected[]`로 사유를 돌려주고 UI가 해제를 제안 | 영구 잠금이면 그 섹션만 낡은 채 남고, 엔진이 매 턴 헛되이 패치를 시도해 토큰을 씀 |
| 08-27 | M2 · §5.2 프롬프트 | 절대 규칙 6번 추가 — locked 섹션은 패치하지 말고 해제를 요청하라 | 잠금 상태를 엔진에 알려야 헛된 패치 반복이 사라짐 |
| 08-27 | M2 · 거부 사유 구분 | `unknown_op` / `unknown_section` / `section_locked` / `malformed` | UI에 뜰 메시지가 사유별로 달라야 함 |
| 08-27 | M2 · 패치 신뢰도 | `patch.ts`에 런타임 타입 가드. 스키마 위반 패치는 버림 | LLM 출력은 신뢰 불가. FR-003 "앱이 죽지 않는다" |
| 08-27 | **엔진 모델 변경** | `claude-opus-5` → **`gemini-3.7-flash`**. Anthropic 안 철회 | 빌더가 Google 키 보유. 앱이 제대로 만들어져 있으면 Gemini로도 충분하다는 판단 |
| 08-27 | 모델 ID 확인 방법 | **Models API 호출로 실측.** 추측 안 함 | 컷오프 이후 모델이라 이름을 알 수 없었음. `gemini-3.1-pro`는 GA에 없고 `-preview`만 존재함을 확인 |
| 08-27 | Pro 티어 배제 | `gemini-3.1-pro-preview`를 쓰지 않음 | 프리뷰는 예고 없이 변경·제거 가능. 앱이 의존하면 위험 |
| 08-27 | **JSON 강제 방식** | op별 배열 분리 (`setSections`/`addOpenQuestions`/…) + SectionId enum | 실측: enum 없으면 `"overview"` 같은 어휘를 지어냄. Gemini `responseSchema`는 `oneOf` 미지원이라 단일 배열로는 op별 필수 필드 강제 불가 |
| 08-27 | thinking 비용 통제 | `thinkingConfig.thinkingBudget: 512` | 실측: 무제한이면 출력의 85%가 thinking(300/354). 512 설정 시 92토큰, 품질 저하 없음 |
| 08-27 | 런타임 가드 유지 | 스키마 강제와 **병행** | 스키마가 있어도 모델이 규약을 벗어남을 실측 확인. M2 가드가 실제로 이를 잡아냄 |
| 08-27 | M3 · 실측 비용 | **턴당 $0.0035** (in=1769, out=585). 30턴 세션 ≈ $0.25 | Opus 5 추정치 $3.15의 1/12 |
| 08-27 | M3 · 재시도 범위 | 스키마 위반 2회 재요청(FR-004), 429·5xx 지수 백오프 3회(§11), 네트워크·인증 오류는 즉시 반환 | NFR-004가 요구한 오류 구분. 5xx는 스펙에 없으나 429와 같은 일시 오류라 함께 처리 |
| 08-27 | M3 · 프롬프트 구조 | 상태 JSON에서 `history` 제외, 최근 6턴은 별도 블록 | §5.1대로. 이력을 두 번 싣지 않는다 |
| 08-27 | M3 · 어댑터 경계 | `runTurn()`이 호출→적용→검증까지. UI는 이것만 안다 | §9 "UI는 callEngine 하나만 안다" |
| 08-27 | M3 · `KRW_PER_USD` | `validate.ts` → `config.ts`로 이동 | 튜닝 값의 단일 출처. TODO는 유지 |
| 08-27 | **M4 · CSS 선택** | `Suggestion: Tailwind`를 쓰지 않고 **플레인 CSS + 커스텀 프로퍼티** | 2단 고정 레이아웃에 CSS 약 200줄. 빌드 체인 의존을 하나 줄이는 편이 단독 개발자에게 유리. 스펙 §9는 Suggestion 변경을 허용 |
| 08-27 | M4 · 상태 관리 | `Suggestion: Zustand 또는 useReducer` 중 **useReducer** | 의존성 0개. 트리가 얕아 Context도 불필요 |
| 08-27 | M4 · 마크다운 렌더링 | 보류. `<pre>` + `white-space: pre-wrap`으로 표시 | FR-006 AC가 색 구분·즉시 갱신·접기펴기만 요구. `react-markdown`은 M6 내보내기와 함께 검토 |
| 08-27 | M4 · 툴체인 충돌 | vitest 3 → 4, vite 8로 동시 상향 | `@vitejs/plugin-react` 6이 vite ^8을 요구해 peer 충돌. 기존 89개 테스트 그대로 통과 |
| 08-27 | M4 · 브라우저 검증 | 실제 턴 왕복 확인 — 턴 1, $0.0042, 차단 15→14, S0·S1 drafting 전환 | 엔진이 대체재(카카오톡 오픈채팅·소모임·밴드)를 지적하고 S0부터 질문. 스펙 §5.2 절대규칙 2·4와 인터뷰 1단계 준수 확인 |
| 08-27 | **순서 변경 M5↔M6** | M6(내보내기)을 M5(영속화)보다 먼저 | 빌더가 9턴짜리 실사용에서 인터뷰를 끝냈는데 내보낼 방법이 없었다. 앱의 산출물이 없으면 나머지가 의미 없다 |
| 08-27 | **버그 · AC 오탐** | `HAS_OBSERVABLE`을 어간+어미 조합으로 재작성. `HAS_CONDITION`에 조사 없는 "실패 시" 추가 | 실사용에서 정상 AC가 22개 오차단됨. `반환한다`만 매칭하고 `반환해야 함`·`합성되어야 함`·`재합성 가능`을 놓쳤다. 범용 동사(제공·동작)는 제외 — 스펙 §6.2가 차단 대상으로 명시한 예시가 통과해버림 |
| 08-27 | **버그 · NFR 미등록** | 시스템 프롬프트에 "요구사항 등록" 절 추가 | 실사용에서 `FR 10 · NFR 0`. 엔진이 S6 본문만 쓰고 `addRequirements`에 section:"NFR"로 등록하지 않아 `NO_NFR`이 영원히 차단 |
| 08-27 | **§13 Q4 해소** | 벤더 사전을 **버전 표기 또는 가격·수치 주장이 있을 때만** 발동하도록 축소 | "Vercel에 배포한다" 같은 단순 언급까지 막으면 S8에 기술 스택을 쓸 수 없어 영원히 내보내기 불가. 스펙 §6.3의 목적은 확정 서술 습관 차단이지 언급 금지가 아님 |
| 08-27 | M6 · 다운로드 방식 | Blob + `URL.createObjectURL`, 1초 뒤 해제 | 즉시 해제하면 일부 브라우저가 저장을 취소함 |
| 08-27 | M6 · 클립보드 실패 대비 | `navigator.clipboard` 실패 시 미리보기 모달로 폴백 | 비 HTTPS·권한 거부 환경에서 복사가 조용히 실패하는 것을 막는다 |
| 08-27 | **FR-014 신설** | 객관식 질문 카드. 보기 + 장단점 + 주관식 칸 | 빌더 요청. 산문 질문은 전부 타이핑해야 해서 답이 짧아지고 누락됨 |
| 08-27 | FR-014 · 장단점 배치 | 보기의 `detail`에 장점/단점을 나란히 | §5.2 절대규칙 4(약점 지적)를 **선택 시점**에 전달하는 자리가 된다 |
| 08-27 | FR-014 · 주관식 판단 | 값을 받아야 하는 질문(예산·기간)은 `options`를 비우게 지시 | 억지 선택지를 막는다. 실측에서 엔진이 Q1(예산)만 주관식으로 알아서 분리 |
| 08-27 | FR-014 · reply 축소 | reply는 "반영 → 지적"까지만. 질문은 카드가 담당 | 같은 질문이 두 번 보이는 것을 막는다 |
| 08-27 | FR-014 · 강제하지 않음 | 카드를 무시하고 자유 입력만으로도 전송 가능 | 사용자가 선택지에 갇히면 안 된다 |
| 08-27 | FR 14개 | §6.4 경고선(12개) 2개 초과 | 빌더 승인. 실사용에서 필요가 확인된 기능 |
| 08-27 | M5 · 저장소 | `idb-keyval`(스펙 §9 Suggestion 그대로). `KV` 인터페이스 뒤에 격리 | IndexedDB는 node에 없다. 테스트는 메모리 구현으로 갈아끼운다 |
| 08-27 | M5 · 세션 메타 분리 | 누적 토큰·답하다 만 카드를 `PRDState`가 아닌 별도 키에 저장 | 문서 모델과 앱 텔레메트리는 다르다. `PRDState`를 오염시키지 않는다 |
| 08-27 | M5 · 실측 발견 | 첫 구현에서 새로고침 시 **누적 비용이 $0으로 리셋**되고 질문 카드가 사라졌다 | FR-012 AC3이 "누적"을 요구하는데 매번 0이면 무의미. 세션 메타 도입으로 해결 |
| 08-27 | M5 · 마이그레이션 | `migrate()`가 없는 필드를 기본값으로 채우고 모르는 건 버린다. **절대 예외를 던지지 않는다** | 저장된 데이터·사용자 파일은 신뢰할 수 없다. 깨져 있어도 앱은 떠야 한다 |
| 08-27 | M5 · 저장 실패 처리 | `saveState`는 false를 돌려주고, 헤더에 "저장 실패" 배지 + 파일 내보내기 안내 | 용량 초과·프라이빗 모드에서 조용히 실패하면 작업을 통째로 잃는다 |
| 08-27 | M5 · 디바운스 600ms | 섹션 직접 편집 중 매 키 입력마다 쓰지 않는다 | |
| 09-02 | **원칙 4 개정** | 검증기의 결과를 **차단 → 고지**로. `canExport()` 폐기, `severity: 'block'` → `'incomplete'` | 빌더 지시 "전부 완성 안 해도 문서는 나오게 하라". 검증 규칙 13종·테스트는 하나도 안 버렸다 — 역할만 문지기에서 점검표로 바뀜 |
| 09-02 | 원칙 4 · 지켜야 할 선 | **건너뛴 항목은 산출물에 `미정`으로 반드시 남는다.** `renderDraftBanner`가 통과 시에도 침묵하지 않음 | 미완성이 문서에서 사라지면 개발 AI가 그 자리를 임의로 채운다. 그러면 이 앱의 존재 이유가 없어짐 |
| 09-02 | FR-005 · 점검 화면 | `ExportGate` 2단(점검 → 받기). [지금 작성]/[건너뛰기]/[모두 건너뛰고 내보내기] | 빌더 지시 "안 한 거 화면에 딱 보여주고 작성하거나 패스하게" |
| 09-02 | 섹션 점프 방식 | `{ id, nonce }` 쌍. rAF로 한 틱 비우는 방식을 쓰지 않음 | 같은 섹션을 연속으로 눌러도 effect가 다시 돌아야 한다. rAF는 프레임 타이밍에 의존해 불필요하게 취약 |
| 09-02 | 진행률 계산 | `completeness()` — 분모 = 필수 섹션 + 전역 규칙 8 + 요구사항 수 | `validate()`는 위반만 돌려주므로 분모를 따로 세야 한다. 규칙 하나가 섹션마다 걸리므로 고정 상수로는 안 됨 |
| 09-02 | 동영상 첨부 | **제외.** 문서·음성·링크만 | 빌더 2차 지시. Files API 브라우저 CORS 확인이 불필요해져 inline 경로만 남음 |
| 09-02 | **FR-016 저장 배치** | `LIBRARY_VERSION`을 **별도 축**으로 신설. `schemaVersion`은 1로 유지 | 개정안 §B2는 "1→2"라 썼으나 `PRDState`의 **모양이 안 바뀌었다.** 문서 스키마와 저장 배치는 다른 축이고, 모양이 그대로인데 올리면 `migrate()`가 헛일을 한다 |
| 09-02 | 판본 찍는 시점 | **게이트를 한 번 열 때 한 번.** 파일별이 아니라 세션별 | 3종을 연달아 받아도 그것은 같은 판본이다. 파일마다 찍으면 v0.4.0까지 튄다 |
| 09-02 | 판본 찍는 조건 | 실제 **다운로드·복사 성공** 시에만. 미리보기는 제외 | 열어만 본 것은 "개발 AI에게 넘겼다"가 아니다 |
| 09-02 | 판본 이력 읽기 | 목록을 열 때가 아니라 **펼칠 때** 문서 본문을 읽는다 | 스냅샷은 `PRDState` 전문이라 무겁다. 목록 렌더에 N개를 전부 읽으면 안 된다 |
| 09-02 | `canExport()` 제거 | 표식으로 남겨뒀던 `true` 반환 함수를 삭제 | 개정안 §A는 "폐기"였다. **가부를 묻는 API가 없는 것**이 "항상 가능하다"의 가장 확실한 보장 |
| 09-02 | 백업 덮어쓰기 | 가져오기는 **덮어쓰지 않는다.** 같은 id면 새 id로 들여온다 | 백업 복원이 현재 작업을 지우면 그것은 복원이 아니라 사고다 |
| 09-02 | **E3 · Mermaid 배제** | 개정안 §B4의 Mermaid 제안을 쓰지 않고 **인라인 SVG**로 직접 그림 | Mermaid는 CDN 스크립트를 요구해 같은 절 AC3(외부 요청 금지)과 정면 충돌. AC가 제안보다 우선한다. `xmlns`도 빼서 산출물에 URL이 0개 |
| 09-02 | E3 · zip 라이브러리 | 안 씀. `export/zip.ts`에 무압축 ZIP 직접 작성 | 산출물이 전부 텍스트라 압축이 무의미하고, store 방식은 헤더 3종이면 끝. 런타임 의존성 3개를 지켰다. `unzip -t`·python `zipfile`로 실측 검증 |
| 09-02 | E3 · 마크다운 렌더링 | M4의 "보류"를 뒤집고 **최소 서브셋 렌더러**를 씀 (`react-markdown` 아님) | 화면 미리보기는 `<pre>`로 충분했지만 **인쇄용 산출물**은 목차·표가 필요하다. 문법 범위가 좁아(우리 엔진이 쓴 것) 70줄로 끝난다 |
| 09-02 | E3 · 조판 순서 | **이스케이프 먼저, 조판 나중** | 섹션 본문은 LLM과 사용자가 쓴다. 둘 다 신뢰하지 않는다. 순서가 반대면 `<img onerror>`가 살아난다 |
| 09-02 | E3 · zip 내부 파일명 | 프로젝트명을 빼고 표준 이름(`PRD.md` 등)만 | 개발 AI가 받는 것은 표준 이름의 폴더여야 한다. 덤으로 구형 Info-ZIP의 한글 파일명 깨짐도 피한다 |
| 09-02 | 산출물 7종 → **8종** | 개정안 §B4 표에 압축본(`PRD-compact.md`)이 누락돼 있었다 | FR-009로 이미 있던 산출물이다. 빼면 기능 후퇴 |
| 09-02 | 버그 · `⭳` 두부 | `⭳`(U+2B73) → `↓`(U+2193) | 시스템 폰트에 없어 브라우저에서 두부(□)로 렌더링되던 것을 실측 발견 |
| 09-02 | 버그 · SVG 과대확대 | `width="100%"` → 자연 크기 + CSS `max-width` | 층이 적은 그래프가 컨테이너 폭까지 확대돼 글자가 거대해졌다. 실측 발견 |
| 09-02 | **표시 이름 분리** | `SectionDef`에 `label`(한글)·`hint`(뭘 쓰는 곳인지) 추가. `title`(영문)은 유지 | 빌더 지시 "S7 이런 게 뭔지 모르겠다". 화면은 `label`, 개발 AI용 산출물은 `S7 + 영문 title` — 후자는 안정적 참조 키라 번역하면 안 된다 |
| 09-02 | 규칙 코드 → 사람 말 | `validator/explain.ts` 신설. 코드마다 `what`(뭐가 문제) + `why`(안 하면 뭐가 잘못되나) | 개정안 §B3 AC2가 요구한 건데 안 지켜져 있었다. **`why`가 없으면 사용자는 전부 건너뛰기를 누른다** — 판단 근거를 줘야 한다 |
| 09-02 | 검증 메시지에서 섹션 이름 제거 | 메시지는 문제만 말하고, 이름은 UI 칩·산출물 굵은 글씨가 붙인다 | 안 그러면 "예외와 실패 상황 · 예외와 실패 상황: 아직…"으로 두 번 나온다. 실측 발견 |
| 09-02 | 사용자 문구 ↔ 진단 분리 | `RejectedPatch.message`(화면) / `.detail`(콘솔) | `S99`·`set_sektion` 같은 값을 사용자에게 보여줄 이유가 없다. 그렇다고 지우면 원인 추적이 죽으므로 콘솔에는 남긴다 |
| 09-02 | 규칙 코드는 `?`로 접기 | 화면엔 물음표만, 코드는 툴팁 | §B3 AC2 "규칙 코드는 부차 정보로만 보인다" |

## 미해결

- **개발 비용은 논외.** Claude Code는 빌더의 Claude Pro 구독에 포함. M1·M2는 LLM 호출이 아예 없다.
- 스펙 §13 Open Questions 중 Q1 해소, **Q2~Q5 미확정** — 해당 코드 착수 직전 사용자 확인 필요.
- **Anthropic API 키 미확보.** 구독과 별개로 console.anthropic.com에서 발급 필요. **M3에서 처음 필요하며 M1·M2에는 지장 없음.**
- `docs/BUILDER_CONTEXT.md`의 예산·배포·가용시간 미기입. M3 전까지 진행에 지장 없음.
- ~~FR-013 출력 포맷 미확정~~ — E3에서 `SETUP.md`로 확정.
- **개정안 #02 승인란이 비어 있다.** E1·E2·E4는 빌더 구두 지시로 구현됐지만 문서상 `⬜`다. 승인 후 `SPEC.md` v3.2로 병합할 것 — 지금은 문서와 코드가 어긋나 있다.
- **E7~E10(계정·결제)는 확정 제약 변경**이라 빌더 외에 누구도 결정할 수 없다. 개정안 §D 스코프 경고 참조.
- **`KRW_PER_USD = 1400`은 임시값** (`validate.ts`). 스펙에 원화 예산 ↔ USD 원가 비교 규정이 없다. M3에서 사용자 확인 후 config로 옮길 것.
- **Google API 키 확보됨** (2026-08-27 동작 확인). 빌더가 `.env`에 `GOOGLE_API_KEY=`로 직접 넣어야 한다 — 에이전트는 `.env` 접근이 deny 규칙으로 막혀 있다.
  - ⚠️ 이 키는 대화 기록에 평문 노출됐다. **개발 종료 후 폐기·재발급 권장.**
  - 앱 자체는 BYOK 유지 (NFR-001, FR-011). `.env`는 개발 테스트 전용이며 앱에 키를 내장하지 않는다.
- **Gemini 단가 `[미검증]`** — $0.75/$3.75는 빌더 제공 자료. aistudio.google.com/pricing 확인 필요. **2027-01-01 2배 인상 예정** caveat 있음.
