# PRD Architect

> **이 파일이 모든 AI 도구의 단일 지침이다.** `CLAUDE.md`와
> `.antigravity/system_instructions.md`는 이 파일을 가리키는 심볼릭 링크다.
> 지침을 고칠 때는 **이 파일만** 고친다.
> (앱이 *생성해서 내보내는* `CLAUDE.md` 산출물은 이것과 무관한 별개다.)

흐릿한 제품 아이디어를 인터뷰로 취조해 **다른 AI가 즉시 개발 착수 가능한 PRD**를 만드는 로컬 우선 웹 앱.
PRD를 "쓰는" 도구가 아니라 **완성 여부를 코드로 검증하고 미완성이면 내보내기를 거부하는** 도구다.

전체 스펙: `docs/SPEC.md` (498줄) — **통째로 읽지 말고 필요한 조항만 찾아 읽을 것.**
진행 상황: `docs/PROGRESS.md` — **세션 시작 시 이것부터 읽는다.**
실사용 결함 기록: `docs/M7-FINDINGS.md`

## 타협 불가 원칙

1. 상태는 앱이 소유. LLM은 기억하지 않는다. 매 턴 상태 JSON을 다시 넣는다.
2. LLM은 전문을 재생성하지 않는다. **패치만 반환**한다.
3. 최종 문서는 생성이 아니라 **조립**(상태 JSON → 템플릿 렌더링).
4. **검증기가 최종 판단자**다. LLM이 "완성"이라 해도 검증기가 미완성이라 하면 미완성이다.
   단 판단의 결과는 **차단이 아니라 고지**다 — 내보내기를 막지 않는다(개정안 #02 §A).
   무엇이 비었는지 점검 화면에 전부 보여주고 뽑을지는 사용자가 정한다.
   **건너뛴 항목은 산출물에 `미정`으로 반드시 남는다. 이 선은 양보하지 않는다.**
5. 미확인 외부 정보(서비스명/모델명/가격/성능 수치)에는 `[미검증]` 태그.

## 개발 규칙

- **FR/NFR ID가 작업 단위.** 커밋 메시지에 ID를 남긴다 (`FR-005: validate() 구현`).
- **Out of Scope는 구현 금지.** 인증·서버DB·결제·웹검색 연동. 좋은 아이디어여도 추가하지 않는다.
- 스펙의 `Suggestion:` 항목만 재량 변경 가능하며 **변경 시 사유를 PROGRESS.md에 기록**. `Suggestion:` 없는 항목은 제약이므로 변경 금지.
- **모델명·가격·API 스펙을 기억으로 쓰지 않는다.** 공식 문서 확인, 불가하면 `config.ts`에 `TODO: 확인 필요` + 사용자 보고.
- 스펙의 Open Questions(§13)는 **해당 코드 작성 직전에** 사용자에게 확인. 임의 결정 금지.
- 스펙 수정이 필요하면 직접 고치지 말고 변경 요청을 사용자에게 제시.
- 각 마일스톤(M1~M7) 완료 시 사용자 확인 후 다음으로 진행.

## 확정 제약 (변경 불가)

- 서버 DB 없음. 클라이언트 영속화만.
- BYOK — API 키는 사용자 소유. 앱에 내장하지 않고 서버로 보내지 않는다.
- 단일 개발자, 소규모 코드베이스.

## 토큰 규율

- `docs/SPEC.md`(498줄) **전문을 읽지 않는다.** 필요한 조항만 찾아 읽는다.
- 파일을 고친 뒤 확인용으로 다시 읽지 않는다.
- 긴 출력(테스트·빌드 로그)은 끝부분만 본다.

## 일을 마쳤다고 말하기 전에

"됐다"고 말하기 전에 **이 셋을 실제로 돌린다.** 안 돌렸으면 안 돌렸다고 말한다.

```
npx tsc --noEmit      # 타입
npx vitest run        # 테스트
npm run build         # 빌드
```

- 화면을 건드렸으면 **브라우저로 눈으로 확인한다** (`npm run dev` → http://localhost:5173).
  이 프로젝트의 결함은 대부분 테스트가 아니라 **실제 화면에서** 나왔다.
- **빌드본은 개발 서버와 다르다.** 배포 전에는 `npm run build && npx vite preview` 로
  `/pre_prd/` 를 열어 본다. 개발 서버만 보고 넘어갔다가 빌드본이 백지였던 적이 있다.
- 테스트가 깨지면 **테스트를 고치지 말고 원인을 고친다.** 기대값을 바꿔야만 한다면
  왜 바꾸는지를 커밋 메시지에 남긴다.
- 실패는 실패라고 보고한다. 통과한 척하지 않는다.

## 커밋과 푸시

- 첫 줄은 **작업 단위 ID로 시작** — `FR-005: …`, `M7: …`, `docs: …`
- 본문에는 **왜 그렇게 했는지**를 쓴다. 무엇을 바꿨는지는 diff가 말한다.
- 꼬리표는 **작업한 도구 이름**으로 남긴다. 남의 이름을 빌리지 않는다.
- **푸시는 사용자가 요청할 때만.** 원격에 올라간 것은 되돌리기 어렵다.
- `main`에서 그대로 작업한다. 브랜치를 새로 파지 않는다(단일 개발자 프로젝트).
- `.env`는 절대 커밋하지 않는다. API 키가 들어 있다.

## 세션을 마칠 때 — 다음 도구가 이어받을 수 있게

이 프로젝트는 **여러 AI 도구가 번갈아 이어받는다.** 다음 사람이 코드베이스를
다시 훑지 않아도 되도록 남긴다. 이것이 인계의 전부다.

- `docs/PROGRESS.md` — 현재 위치 · 다음 할 일 · 결정 한 줄 · 미해결
- `docs/M7-FINDINGS.md` — 실사용 결함 (증상 → 원인 → 수정 → 확인 방법)
- **사용자가 정해야 할 것은 "빌더 판단 대기"로 남긴다.** 대신 정하지 않는다.
- 하던 일이 있으면 **어디까지 했는지** 적는다. 커밋 안 된 파일을 남기고 끝내지 않는다.

## 도구별 차이

| 하려는 일 | Claude Code | 그 외 (Antigravity 등) |
|---|---|---|
| 스펙 조항 찾기 | `spec` 서브에이전트 | `grep -n "§6.2" docs/SPEC.md` — 전문은 읽지 않는다 |
| 테스트 실행·진단 | `tests` 서브에이전트 | 직접 돌리고 **실패한 부분만** 읽는다 |
| 세션 마무리 | `/wrap` | 위 "세션을 마칠 때"를 손으로 수행 |

서브에이전트나 슬래시 명령이 없는 도구는 **오른쪽 열대로** 하면 된다.
이 지침의 목적은 도구 사용법이 아니라 **컨텍스트를 아끼는 것**이다.
