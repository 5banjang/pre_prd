# 지금 할 일

> **모든 AI는 작업을 시작하기 전에 이 파일부터 읽는다.**
> 규칙은 `AGENTS.md`의 "이어받기 규칙" 참고. 이 파일은 60줄을 넘기지 않는다.
> 코드 지도·전체 배경은 `docs/PROGRESS.md`(길다. 필요한 부분만 찾아 읽을 것).

- **상태**: 진행중   <!-- 비어 있음 / 진행중 / 막힘 -->
- **맡은 AI**: -
- **마지막 갱신**: 2026-09-04

## 무엇을
**시작 화면에 "자료 첨부로 시작" 갈래 추가.** FR-015(자료 첨부)가 완성됐으니 이제 붙일 수 있다.

## 왜
지금은 빈 화면에서 인터뷰만 시작할 수 있다. 이미 기획서·메모가 있는 사람은
그걸 먼저 넣고 시작하는 편이 훨씬 빠르다.

## 단계
- [ ] 시작 화면(첫 진입 UI)에 갈래 두 개: [그냥 시작] / [자료 첨부로 시작]
- [ ] 첨부로 시작하면 FR-015 첨부 흐름을 태우고, 첫 턴 프롬프트에 요약을 넣는다
- [ ] 테스트 추가 후 `npm test`, `tsc --noEmit`, `npm run build` 모두 클린 확인

## 완료 기준
- 새로고침·판본 찍기까지 정상, 테스트 전부 통과

## 지금까지 알아낸 것
- **M7(34턴 실사용 검증)은 빌더만 진행할 수 있다** — API 키 입력이 필요하다. 8/30턴 진행됨.
  남은 22턴에서 볼 것: 장기 세션 컨텍스트 유실, `trimHistory` 실동작, 25턴 안내(FR-012 AC2),
  압축본 4,000자 상한, "모르겠음 처리"가 실제로 지켜지는지
- **E7~E10(계정·결제)는 개정안 #03이 필요하다.** 확정 제약 3개(서버 DB 없음·BYOK·
  결제 Out of Scope)를 동시에 푸는 일이라 코드부터 손대면 안 된다. 빌더와 먼저 합의할 것
- 빌드본이 백지로 뜨던 문제는 `vite preview`의 base 어긋남이었다(해결됨)

## 손댄 파일
- (아직 없음)
